# Main requirements (for local development)
# Note: API functions use api/requirements.txt to reduce bundle size

requests>=2.28.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0
fantasy-rankings-scraper>=0.0.4
